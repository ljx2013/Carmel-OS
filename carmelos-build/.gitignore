# Build artifacts (regenerated by `lb build`) — do not commit
.build/
.cache/
chroot/
chroot.debconf
chroot.files
chroot.packages.install
chroot.packages.live
binary/
binary.iso
binary.contents
binary.packages
build.log
*.iso

# Local apt cache
local/

# Keep: auto/, config/, and anything committed manually
